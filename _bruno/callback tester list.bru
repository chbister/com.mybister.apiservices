meta {
  name: callback tester list
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/callback-tester/list
  body: json
  auth: inherit
}

body:json {
  { "foobar": "barfoo"}
}

settings {
  encodeUrl: true
  timeout: 0
}
